# Startup
if [ -f ~/.bashrc ]; then
  . ~/.bashrc
fi

if [ -f "${HOME}/.profile" ]; then
  . "${HOME}/.profile"
fi

export EDITOR=nvim
export SUDO_EDITOR=nvim
export VISUAL=nvim

. "$HOME/.cargo/env"
