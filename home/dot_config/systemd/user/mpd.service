[Install]
WantedBy=default.target

[Service]
ExecStart=/usr/bin/mpd --no-daemon %h/.config/mpd/mpd.conf
ExecStartPre=/usr/bin/mkdir -p %h/.local/share/mpd %h/Music/Playlists
Type=notify

[Unit]
After=network.target
After=sound.target
Description=Music Player Daemon
