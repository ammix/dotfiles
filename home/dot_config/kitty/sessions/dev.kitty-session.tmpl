# Main dev workspace tabs
new_tab dotfiles
cd "{{ joinPath .chezmoi.sourceDir ".." }}"
launch

new_tab nvim
cd "{{ joinPath .chezmoi.sourceDir "dot_config/nvim" }}"
launch

new_tab ammix-os
cd ~/ammix-os
launch
